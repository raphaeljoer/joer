describe('CheckinVehicle', () => {
  test('Should make chekin', () => {
    // const checkin = new CheckinVehicle({ uuid, ticketRepository });
    // const checkinDate = new Date('2022-01-01T10:00');
    // const ticket = checkin.execute({ vehicle, checkinDate });
    // expect(ticket.checkinDate).toBe(checkinDate);
  });
});
